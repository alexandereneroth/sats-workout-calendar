<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->


        <item name="colorPrimary">@color/actionbar_background</item>
    </style>

    <style name="Widget.Sats.StickyListViewHeader" parent="@android:style/Widget.ListView">
        <item name="android:paddingLeft">@dimen/activity_vertical_margin</item>
        <item name="android:paddingRight">@dimen/activity_vertical_margin</item>
        <item name="android:colorBackground">@color/light_gray</item>
    </style>

</resources>
